<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="team.list">
      <field name="name">team list</field>
      <field name="model">team.team</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="team.action_window">
      <field name="name">team window</field>
      <field name="res_model">team.team</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="team.action_server">
      <field name="name">team server</field>
      <field name="model_id" ref="model_team_team"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="team" id="team.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="team.menu_1" parent="team.menu_root"/>
    <menuitem name="Menu 2" id="team.menu_2" parent="team.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="team.menu_1_list" parent="team.menu_1"
              action="team.action_window"/>
    <menuitem name="Server to list" id="team" parent="team.menu_2"
              action="team.action_server"/>
    -->
  </data>
</odoo>